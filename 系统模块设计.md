# 广州城市卡片系统模块设计

## 1. 系统架构

### 1.1 技术栈
- **前端框架**：Next.js 14（App Router）
- **后端语言**：TypeScript
- **数据库**：MySQL
- **ORM**：Prisma
- **认证**：JWT + Session
- **样式**：Tailwind CSS
- **部署**：Vercel（推荐）

### 1.2 架构模式
采用 **分层架构**，具体包括：
- **表示层**：Next.js 页面和组件
- **业务逻辑层**：API 路由处理函数
- **数据访问层**：Prisma Client
- **数据存储层**：MySQL 数据库

## 2. 系统功能模块设计

### 2.1 系统功能模块图

广州城市卡片系统的功能模块主要包括用户登录注册模块、景点模块、美食模块、文化模块、新闻模块、经济模块、个人中心模块以及管理中心模块。

### 2.2 用户登录注册模块

用户登录注册模块是系统的基础模块，负责用户的注册、登录、信息维护及管理员对用户的管理操作。

#### 2.2.1 用户注册流程
用户注册采用邮箱密码方式。用户在前端注册页面填写邮箱和密码，前端对输入进行基本校验后将请求发送至后端。后端接收到注册请求后，首先校验邮箱是否已存在、密码长度是否符合要求，然后对密码进行加盐哈希加密处理，最后将用户信息写入数据库并返回注册结果。

**用户注册时序图**：
1. 用户在前端填写注册信息并提交
2. 前端对输入进行基本校验
3. 前端发送注册请求至 `/api/auth/register`
4. 后端 API 路由接收请求
5. 调用认证服务验证邮箱是否已存在
6. 对密码进行加密处理
7. 创建用户记录到数据库
8. 生成 JWT token
9. 返回注册成功信息和 token

#### 2.2.2 用户登录流程
用户登录同样采用邮箱密码方式。前端将邮箱和密码提交至后端，后端校验邮箱密码正确性后，通过 JWT 框架生成登录 Token 并返回给前端，实现会话管理。

**用户登录时序图**：
1. 用户在前端填写登录信息并提交
2. 前端发送登录请求至 `/api/auth/login`
3. 后端 API 路由接收请求
4. 调用认证服务验证邮箱和密码
5. 验证通过后生成 JWT token
6. 返回登录成功信息和 token
7. 前端存储 token 用于后续请求

#### 2.2.3 用户管理接口设计

| 接口路径 | 请求方法 | 功能说明 | 是否认证 |
|---------|---------|---------|---------|
| `/api/auth/register` | POST | 用户注册 | 无需登录 |
| `/api/auth/login` | POST | 用户登录 | 无需登录 |
| `/api/auth/logout` | POST | 用户退出登录 | 已登录 |
| `/api/auth/me` | GET | 获取当前登录用户信息 | 已登录 |
| `/api/auth/refresh` | POST | 刷新 token | 已登录 |
| `/api/auth/reset-password` | POST | 重置密码 | 无需登录 |
| `/api/auth/send-verification` | POST | 发送验证邮件 | 已登录 |
| `/api/users/profile` | PUT | 更新用户资料 | 已登录 |
| `/api/admin/users` | GET | 获取用户管理列表 | 管理员 |
| `/api/admin/users/[id]/role` | PUT | 更新用户角色 | 管理员 |

用户信息通过响应数据结构进行脱敏处理，普通用户只能获取到脱敏后的用户信息（如昵称、头像等），密码等敏感字段不会返回给前端。

### 2.3 景点模块

景点模块是系统的核心模块之一，负责广州景点的创建、编辑、删除、搜索和展示。

#### 2.3.1 景点搜索流程
系统提供了基于 MySQL 的多条件组合搜索能力。用户在搜索框输入关键词后，系统支持按景点名称、地址、分类等多条件组合搜索。搜索请求首先到达后端 API 路由，路由调用景点服务的搜索方法，服务层通过 Prisma 的查询构建器构建动态查询条件，支持关键词模糊匹配、分类筛选等多维度检索，并通过分页实现高效的分页查询。

**景点搜索活动图**：
1. 用户在前端输入搜索关键词和筛选条件
2. 前端发送搜索请求至 `/api/attractions`
3. 后端 API 路由接收请求参数
4. 构建 Prisma 查询条件
5. 执行数据库查询
6. 处理查询结果（分页、格式化）
7. 返回搜索结果给前端
8. 前端渲染搜索结果列表

#### 2.3.2 景点管理接口设计

| 接口路径 | 请求方法 | 功能说明 | 是否认证 |
|---------|---------|---------|---------|
| `/api/attractions` | GET | 获取景点列表（支持搜索筛选） | 无需登录 |
| `/api/attractions/[id]` | GET | 获取景点详情 | 无需登录 |
| `/api/attractions/[id]/favorite` | POST | 收藏/取消收藏景点 | 已登录 |
| `/api/attractions/[id]/comments` | GET | 获取景点评论 | 无需登录 |
| `/api/attractions/[id]/comments` | POST | 发布景点评论 | 已登录 |
| `/api/attractions/[id]/comments/[commentId]/like` | POST | 点赞/取消点赞评论 | 已登录 |
| `/api/admin/attractions` | GET | 获取景点管理列表 | 管理员 |
| `/api/admin/attractions` | POST | 创建景点 | 管理员 |
| `/api/admin/attractions/[id]` | PUT | 更新景点 | 管理员 |
| `/api/admin/attractions/[id]` | DELETE | 删除景点 | 管理员 |

### 2.4 美食模块

美食模块负责广州特色美食的创建、编辑、删除、搜索和展示。

#### 2.4.1 美食浏览流程
用户可以浏览美食列表，查看美食详情，收藏喜欢的美食，并对美食进行评论和点赞。

**美食浏览时序图**：
1. 用户访问美食列表页面
2. 前端请求 `/api/foods` 获取美食列表
3. 后端返回美食列表数据
4. 用户点击某个美食查看详情
5. 前端请求 `/api/foods/[id]` 获取美食详情
6. 后端返回美食详情数据
7. 前端渲染美食详情页面
8. 用户可以收藏、评论或点赞

#### 2.4.2 美食管理接口设计

| 接口路径 | 请求方法 | 功能说明 | 是否认证 |
|---------|---------|---------|---------|
| `/api/foods` | GET | 获取美食列表（支持搜索筛选） | 无需登录 |
| `/api/foods/[id]` | GET | 获取美食详情 | 无需登录 |
| `/api/foods/[id]/favorite` | POST | 收藏/取消收藏美食 | 已登录 |
| `/api/foods/[id]/comments` | GET | 获取美食评论 | 无需登录 |
| `/api/foods/[id]/comments` | POST | 发布美食评论 | 已登录 |
| `/api/foods/[id]/comments/[commentId]/like` | POST | 点赞/取消点赞评论 | 已登录 |
| `/api/admin/food` | GET | 获取美食管理列表 | 管理员 |
| `/api/admin/food` | POST | 创建美食 | 管理员 |
| `/api/admin/food/[id]` | PUT | 更新美食 | 管理员 |
| `/api/admin/food/[id]` | DELETE | 删除美食 | 管理员 |

### 2.5 文化模块

文化模块负责广州文化的分类管理和文化项目的创建、编辑、删除、搜索和展示。

#### 2.5.1 文化项目浏览流程
用户可以浏览文化分类，查看文化项目详情，收藏感兴趣的文化项目，并对文化项目进行评论和点赞。

**文化项目浏览时序图**：
1. 用户访问文化分类页面
2. 前端请求 `/api/culture` 获取文化分类列表
3. 后端返回文化分类数据
4. 用户点击某个文化分类
5. 前端请求 `/api/culture/[id]` 获取分类详情和项目列表
6. 后端返回分类详情和项目数据
7. 用户点击某个文化项目查看详情
8. 前端请求 `/api/culture/[id]/[itemId]` 获取项目详情
9. 后端返回项目详情数据
10. 前端渲染项目详情页面

#### 2.5.2 文化管理接口设计

| 接口路径 | 请求方法 | 功能说明 | 是否认证 |
|---------|---------|---------|---------|
| `/api/culture` | GET | 获取文化分类列表 | 无需登录 |
| `/api/culture/[id]` | GET | 获取文化分类详情 | 无需登录 |
| `/api/culture/[id]/[itemId]` | GET | 获取文化项目详情 | 无需登录 |
| `/api/culture/[id]/[itemId]/favorite` | POST | 收藏/取消收藏文化项目 | 已登录 |
| `/api/culture/[id]/[itemId]/comments` | GET | 获取文化项目评论 | 无需登录 |
| `/api/culture/[id]/[itemId]/comments` | POST | 发布文化项目评论 | 已登录 |
| `/api/culture/[id]/[itemId]/comments/[commentId]/like` | POST | 点赞/取消点赞评论 | 已登录 |
| `/api/admin/culture` | GET | 获取文化管理列表 | 管理员 |
| `/api/admin/culture` | POST | 创建文化分类 | 管理员 |
| `/api/admin/culture/[id]` | PUT | 更新文化分类 | 管理员 |
| `/api/admin/culture/[id]` | DELETE | 删除文化分类 | 管理员 |
| `/api/admin/culture/[id]/items` | POST | 创建文化项目 | 管理员 |
| `/api/admin/culture/[id]/items/[itemId]` | PUT | 更新文化项目 | 管理员 |
| `/api/admin/culture/[id]/items/[itemId]` | DELETE | 删除文化项目 | 管理员 |

### 2.6 新闻模块

新闻模块负责广州相关新闻的发布、编辑、删除和展示。

#### 2.6.1 新闻浏览流程
用户可以浏览新闻列表，查看新闻详情。

**新闻浏览时序图**：
1. 用户访问新闻列表页面
2. 前端请求 `/api/news` 获取新闻列表
3. 后端返回新闻列表数据
4. 用户点击某个新闻查看详情
5. 前端请求 `/api/news/[id]` 获取新闻详情
6. 后端返回新闻详情数据
7. 前端渲染新闻详情页面

#### 2.6.2 新闻管理接口设计

| 接口路径 | 请求方法 | 功能说明 | 是否认证 |
|---------|---------|---------|---------|
| `/api/news` | GET | 获取新闻列表 | 无需登录 |
| `/api/news/[id]` | GET | 获取新闻详情 | 无需登录 |
| `/api/admin/news` | GET | 获取新闻管理列表 | 管理员 |
| `/api/admin/news` | POST | 创建新闻 | 管理员 |
| `/api/admin/news/[id]` | PUT | 更新新闻 | 管理员 |
| `/api/admin/news/[id]` | DELETE | 删除新闻 | 管理员 |

### 2.7 经济模块

经济模块负责广州经济相关信息的发布、编辑、删除和展示。

#### 2.7.1 经济信息浏览流程
用户可以浏览经济信息列表，查看经济信息详情。

**经济信息浏览时序图**：
1. 用户访问经济信息列表页面
2. 前端请求 `/api/economy` 获取经济信息列表
3. 后端返回经济信息列表数据
4. 用户点击某个经济信息查看详情
5. 前端请求 `/api/economy/[id]` 获取经济信息详情
6. 后端返回经济信息详情数据
7. 前端渲染经济信息详情页面

#### 2.7.2 经济信息管理接口设计

| 接口路径 | 请求方法 | 功能说明 | 是否认证 |
|---------|---------|---------|---------|
| `/api/economy` | GET | 获取经济信息列表 | 无需登录 |
| `/api/economy/[id]` | GET | 获取经济信息详情 | 无需登录 |
| `/api/admin/economy` | GET | 获取经济信息管理列表 | 管理员 |
| `/api/admin/economy` | POST | 创建经济信息 | 管理员 |
| `/api/admin/economy/[id]` | PUT | 更新经济信息 | 管理员 |
| `/api/admin/economy/[id]` | DELETE | 删除经济信息 | 管理员 |

### 2.8 个人中心模块

个人中心模块负责用户个人信息的管理和用户收藏内容的管理。

#### 2.8.1 个人中心功能流程
用户可以查看和修改个人信息，管理收藏的景点、美食和文化项目。

**个人中心功能时序图**：
1. 用户登录系统
2. 点击个人中心入口
3. 前端请求 `/api/users/profile` 获取个人信息
4. 后端返回个人信息数据
5. 前端请求 `/api/users/favorites` 获取收藏内容
6. 后端返回收藏内容数据
7. 前端渲染个人中心页面
8. 用户可以修改个人信息或管理收藏内容

#### 2.8.2 个人中心接口设计

| 接口路径 | 请求方法 | 功能说明 | 是否认证 |
|---------|---------|---------|---------|
| `/api/users/profile` | GET | 获取个人信息 | 已登录 |
| `/api/users/profile` | PUT | 更新个人信息 | 已登录 |
| `/api/users/password` | PUT | 修改密码 | 已登录 |
| `/api/users/favorites` | GET | 获取收藏内容 | 已登录 |
| `/api/users/favorites/[type]/[id]` | DELETE | 删除收藏 | 已登录 |

### 2.9 管理中心模块

管理中心模块负责系统内容的管理，包括用户管理、景点管理、美食管理、文化管理、新闻管理和经济信息管理。

#### 2.9.1 管理中心功能流程
管理员登录系统后，可以进入管理中心，对系统内容进行管理。

**管理中心功能时序图**：
1. 管理员登录系统
2. 点击管理中心入口
3. 前端请求 `/api/admin/dashboard` 获取管理中心数据
4. 后端返回管理中心数据
5. 前端渲染管理中心页面
6. 管理员可以管理系统内容

#### 2.9.2 管理中心接口设计

| 接口路径 | 请求方法 | 功能说明 | 是否认证 |
|---------|---------|---------|---------|
| `/api/admin/users` | GET | 获取用户管理列表 | 管理员 |
| `/api/admin/users/[id]/role` | PUT | 更新用户角色 | 管理员 |
| `/api/admin/attractions` | GET | 获取景点管理列表 | 管理员 |
| `/api/admin/attractions` | POST | 创建景点 | 管理员 |
| `/api/admin/attractions/[id]` | PUT | 更新景点 | 管理员 |
| `/api/admin/attractions/[id]` | DELETE | 删除景点 | 管理员 |
| `/api/admin/foods` | GET | 获取美食管理列表 | 管理员 |
| `/api/admin/foods` | POST | 创建美食 | 管理员 |
| `/api/admin/foods/[id]` | PUT | 更新美食 | 管理员 |
| `/api/admin/foods/[id]` | DELETE | 删除美食 | 管理员 |
| `/api/admin/culture` | GET | 获取文化管理列表 | 管理员 |
| `/api/admin/culture` | POST | 创建文化分类 | 管理员 |
| `/api/admin/culture/[id]` | PUT | 更新文化分类 | 管理员 |
| `/api/admin/culture/[id]` | DELETE | 删除文化分类 | 管理员 |
| `/api/admin/culture/[id]/items` | POST | 创建文化项目 | 管理员 |
| `/api/admin/culture/[id]/items/[itemId]` | PUT | 更新文化项目 | 管理员 |
| `/api/admin/culture/[id]/items/[itemId]` | DELETE | 删除文化项目 | 管理员 |
| `/api/admin/categories` | GET | 获取分类管理列表 | 管理员 |
| `/api/admin/categories` | POST | 创建分类 | 管理员 |

## 3. 数据流程

### 3.1 用户认证流程
1. 用户注册：提交注册信息 → 验证邮箱 → 创建用户 → 登录
2. 用户登录：提交登录信息 → 验证凭证 → 生成 JWT → 返回 token
3. 访问保护资源：携带 token → 验证 token → 授权访问

### 3.2 内容管理流程
1. 管理员登录 → 进入管理后台 → 创建/编辑内容 → 提交保存 → 数据更新
2. 前台用户浏览 → 查看详情 → 收藏/评论/点赞 → 数据记录

### 3.3 数据查询流程
1. 前台请求数据 → API 接收请求 → 验证权限 → 数据库查询 → 数据处理 → 返回结果
2. 后台管理请求 → API 接收请求 → 验证管理员权限 → 数据库查询 → 数据处理 → 返回结果

## 4. 系统安全

### 4.1 认证安全
- 使用 JWT 进行身份验证
- 密码加密存储
- 邮箱验证机制
- 密码重置流程

### 4.2 授权安全
- 基于角色的访问控制（RBAC）
- API 权限验证
- 敏感操作权限控制

### 4.3 数据安全
- 输入验证和过滤
- SQL 注入防护（使用 Prisma ORM）
- XSS 防护
- CSRF 防护

### 4.4 文件安全
- 文件类型验证
- 文件大小限制
- 文件存储安全

## 5. 部署方案

### 5.1 本地开发
- 环境配置：Node.js 18+
- 数据库：本地 MySQL 或 Docker
- 运行命令：`npm run dev`

### 5.2 生产部署
- 推荐平台：Vercel
- 数据库：MySQL（推荐 PlanetScale 或 AWS RDS）
- 环境变量配置：DATABASE_URL、JWT_SECRET 等
- 自动构建和部署

## 6. 性能优化

### 6.1 前端优化
- 静态页面生成（SSG）
- 服务器端渲染（SSR）
- 客户端缓存
- 图片优化

### 6.2 后端优化
- 数据库索引
- 查询优化
- 缓存策略
- 负载均衡

### 6.3 数据库优化
- 合理的表结构设计
- 适当的索引
- 定期维护

## 7. 扩展性考虑

### 7.1 功能扩展
- 支持多语言
- 添加更多内容类型（如酒店、购物等）
- 集成第三方服务（如地图、支付等）

### 7.2 技术扩展
- 微服务架构
- 容器化部署
- 自动化测试
- CI/CD 流程

## 8. 总结

广州城市卡片系统是一个综合性的城市信息平台，通过模块化设计实现了用户管理、内容管理、互动功能等核心功能。系统采用现代化的技术栈，具有良好的可扩展性和维护性。

通过本设计文档，系统的架构、模块划分、功能实现和技术方案得到了清晰的定义，为后续的开发和维护提供了指导。